import{_ as r}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as o,a as E,o as d}from"./app-BLsriB6k.js";const b="/htblog/images/szkingdom/szbackend1.png",a={};function l(c,e){return d(),o("div",null,e[0]||(e[0]=[E('<h2 id="需求分析" tabindex="-1"><a class="header-anchor" href="#需求分析"><span>需求分析</span></a></h2><h3 id="需求分析方法" tabindex="-1"><a class="header-anchor" href="#需求分析方法"><span>需求分析方法</span></a></h3><p>3图1表：流程图、用例图、时序图、功能列表<br><em><strong>流程图</strong></em><br> 四要素：</p><ul><li>对象：即执行人或产品中的用户。</li><li>输入：既可以是事件，也可以是动作。可以理解为前置条件。</li><li>动作：即产品中的操作，如点击、输入等。</li><li>输出：即结果或动作的目的。<br> 一个流程会将这4个要素有序串联起来，而流程图则是承载上述程序的图形载体。</li></ul><p>分类：根据流程图“流动”信息的不同，又可以细分为业务流程图、功能流程图、数据流程图等。<br> 三大结构：顺序结构、选择结构、循环结构<br> 路径规范<br> （1）绘制流程图时，为了提高流程图的逻辑性，应遵循从左到右、从上到下的顺序排列。<br> （2）一个流程从开始符开始，以结束符结束。开始符号只能出现一次，而结束符号可出现多次。若流程足够清晰，可省略开始、结束符号。<br> （3）同一流程图内，符号大小需要保持一致，连接线不能无故弯曲。<br> （4）流程处理关系为并行关系的，需要将流程放在同一高度。<br> （5）处理流程须以单一入口和单一出口绘制，同一路径的指示箭头应只有一个。</p><p><em><strong>用例图</strong></em><br> 由参与者（Actor）、用例（Use Case）以及它们之间的关系构成的用于描述系统功能的动态视图。<br><em><strong>时序图</strong></em><br> 时序图通过描述对象之间发送消息的时间顺序显示多个对象之间的交互模式。<br> 纵向：时间轴，时间沿竖线向下延伸；<br> 横向：代表了在协作中各独立对象的角色。</p><p>时序图元素<br> 角色(Actor)：系统角色，可以是人或者其他系统，子系统。以一个小人图标表示。<br> 对象（Object）：代表时序图中的对象在交互中所扮演的角色，一般借用矩形表示。<br> 生命线（lifeline）：代表时序图中的对象在一段时期内的存在。<br> 会话(Activation)：也叫激活框，它代表在对象生命线上某个阶段执行的操作，以一个偏窄的矩形表示。<br> 消息（Message）：对象之间发送的信息。消息分为3种，同步消息、异步消息、返回消息。<br> 组合片段（Fragments）:组合片段表示的是多个操作形成的组合操作，用于表达更加丰富的操作，常见的是循环、判断。</p><p>常用组合片段<br> 抉择：或称条件分支，可根据不同的条件，执行不同的交互，类似于if-else语句。<br> 循环片段：表明该片段会被重复执行，类似于while或for语句。<br> 并行：并行处理，片段中的事件可以并行交错。相当于多线程。</p><p><em><strong>功能列表</strong></em><br> 功能列表以列表的形式详细列出了软件系统将要实现的所有功能。<br> 常用的要素：功能ID、功能模块、功能名称、功能描述、优先级、关联功能、责任人、状态<br> 功能性需求：<br> 功能需求描述了软件应该做什么，它们定义了系统必须具备的功能或特性。这可以是计算、数据操作、业务流程、用户交互或其他任何定义系统可能执行的功能的特定功能。<br> 非功能性需求：<br> 是指依靠一些条件判断系统运作情形或其特性，而不是针对系统特定行为的需求。包括安全性、可靠性、互操作性、健壮性、易使用性、可维护性、可移植性、可重用性、可扩充性等。</p><figure><img src="'+b+'" alt="支付-时序图" tabindex="0" loading="lazy"><figcaption>支付-时序图</figcaption></figure><h2 id="代码规范" tabindex="-1"><a class="header-anchor" href="#代码规范"><span>代码规范</span></a></h2><p><em><strong>高质量的代码编写原则：</strong></em></p><ul><li>每个变量只用于单一用途</li><li>每一行代码只表达一件事</li><li>一个循环只做一件事</li><li>单一抽象层次原则</li><li>函数应该遵守单一职责</li><li>函数圈复杂度应该小于10</li><li>函数第一原则是必须要短小</li><li>编写函数时必须一心一意、专注、怀有谦虚的心态</li></ul><h2 id="数据库" tabindex="-1"><a class="header-anchor" href="#数据库"><span>数据库</span></a></h2><p><em><strong>单表查询（DQL）</strong></em><br> 一、<em><strong>SELECT 语句基础</strong></em></p><ol><li>查询所有字段<br><code>SELECT * FROM employees;</code><br> 注意：* 会返回所有列，可能影响性能，建议在生产环境中指定具体字段。</li><li>指定字段查询<br><code>SELECT first_name, salary FROM employees;</code><br> 优点：减少数据传输量，提升查询效率。</li><li>别名与计算<br><code>SELECT first_name AS name, salary * 12 AS annual_salary FROM employees;</code><br> 别名（AS）：简化字段名或表名，增强可读性。<br> 计算：支持算术运算（+, -, *, /）和表达式。</li><li>去重查询（DISTINCT）<br><code>SELECT DISTINCT department_id FROM employees;</code><br> 注意：DISTINCT 作用于整行数据，多列去重时需组合字段。<br> 二、<em><strong>WHERE 条件过滤</strong></em></li><li>比较运算符<br><code>SELECT * FROM employees WHERE salary &gt; 5000;</code><br><code>SELECT * FROM employees WHERE job_title = &#39;Manager&#39;;</code><br> 常用运算符：=, &gt;, &lt;, &gt;=, &lt;=, &lt;&gt;（不等于）。</li><li>范围与集合<br><code>SELECT * FROM employees WHERE salary BETWEEN 3000 AND 8000;</code><br><code>SELECT * FROM employees WHERE department_id IN (10, 20);</code><br> BETWEEN：包含边界值（如 3000 和 8000）。<br> IN：匹配集合中的任意一个值</li><li>模糊匹配（LIKE）<br><code>SELECT * FROM employees WHERE first_name LIKE &#39;A%&#39;;</code><br><code>SELECT * FROM employees WHERE first_name LIKE &#39;%Smith%&#39;;</code><br><code>SELECT * FROM employees WHERE first_name LIKE &#39;_a%&#39;;</code><br> 通配符：<br> %：匹配任意长度的字符（包括空字符）。<br> _：匹配单个字符。</li><li>空值判断<br><code>SELECT * FROM employees WHERE commission IS NULL;</code><br><code>SELECT * FROM employees WHERE commission IS NOT NULL;</code><br> 注意：NULL 表示缺失值，不能使用 = NULL 或 &lt;&gt; NULL 判断。<br> 三、排序（ORDER BY）<br><code>SELECT first_name, salary FROM employees ORDER BY salary ASC;</code><br><code>SELECT first_name, salary FROM employees ORDER BY salary DESC;</code><br><code>SELECT department_id, salary FROM employees ORDER BY department_id ASC, salary DESC;</code><br> 排序规则：<br> ASC：升序（默认）。<br> DESC：降序。<br> 多字段排序：逗号分隔字段，优先级从左到右递减。<br> 四、综合示例<br> 示例 1：查询高薪员工<br> -- 查询工资高于 8000 且部门为 20 的员工，按工资降序排列<br><code>SELECT first_name, salary, department_id FROM employees WHERE salary &gt; 8000 AND department_id = 20 ORDER BY salary DESC;</code><br> 示例 2：模糊查询与去重<br> -- 查询名字包含 &#39;John&#39; 的唯一职位名称<br><code>SELECT DISTINCT job_title FROM employees WHERE first_name LIKE &#39;%John%&#39;;</code><br> 示例 3：计算与别名<br> -- 查询员工姓名、工资、年薪，并为年薪设置别名<br><code>SELECT first_name AS name, salary, salary * 12 AS annual_salary FROM employees;</code><br> 五、注意事项<br> 性能优化：<br> 避免使用 SELECT *，仅选择必要字段。<br><strong>在大数据表中，LIKE 以 % 开头（如 `&#39;%&#39;）可能导致全表扫描，影响性能</strong>。<br> 空值处理：<br> NULL 值不会被 WHERE 条件中的 =, &lt;&gt; 等运算符匹配，必须使用 <strong>IS NULL 或 IS NOT NULL</strong>。<br> 逻辑运算符优先级：<br><strong>AND 优先于 OR</strong>，复杂条件需使用括号明确逻辑（如 (条件1 AND 条件2) OR 条件3）。</li></ol><p>数据操作（DML）<br> 一、插入数据（INSERT）</p><ol><li>单条插入<br> -- 插入单条记录，指定字段<br><code>INSERT INTO employees (employee_id, first_name, salary, department_id) VALUES (101, &#39;John Doe&#39;, 5000, 20);</code><br> -- 插入单条记录，省略字段名（需按表结构顺序提供值）<br><code>INSERT INTO employees VALUES (102, &#39;Jane Smith&#39;, 6000, 30); </code><br> 注意事项：<br> 字段顺序需与值顺序一致。<br> 字符串和日期需用单引号包裹（如 &#39;2025-05-25&#39;）。<br> 主键或唯一约束字段的值不能重复。<br> 二、更新数据（UPDATE）</li><li>基础语法<br><code>UPDATE employees SET salary = 5500 WHERE employee_id = 101;</code><br><code>UPDATE employees SET salary = salary * 1.1, department_id = 30 WHERE department_id = 20;</code><br> 注意事项：<br> 必须带 WHERE 条件，否则会导致全表更新（所有行数据被修改）。<br> 可使用表达式（如 salary * 1.1）或子查询更新数据。</li><li>高级用法（结合子查询）<br> -- 根据其他表更新数据<br><code>UPDATE employees SET salary = salary * 1.05 </code><br><code>WHERE department_id IN (SELECT department_id FROM departments WHERE location_id = 1700);</code><br> 三、删除数据（DELETE）</li><li>基础语法<br><code>DELETE FROM employees WHERE salary &lt; 3000;</code><br> 注意事项：<br> 必须带 WHERE 条件，否则会删除全表数据。<br> 删除操作是可回滚的（通过事务控制）。</li><li>TRUNCATE<br><code>TRUNCATE TABLE employees;</code><br> 注意：<br> 1）DELETE 每行操作记录日志，可回滚；而 TRUNCATE 不记录单行操作，不可回滚<br> 2）DELETE 性能较慢（逐行删除）；而TRUNCATE 性能极佳（释放数据页）。<br> 四、综合示例：<br> 插入后立即更新<br> -- 插入新员工<br><code>INSERT INTO employees (employee_id, first_name, salary, department_id) VALUES (107, &#39;Eve&#39;, 4000, 40);</code><br> -- 更新其薪资<br><code>UPDATE employees SET salary = 4200 WHERE employee_id = 107;</code><br> 五、注意事项<br> 执行 DELETE 或 UPDATE 前，使用 SELECT 验证 WHERE 条件是否正确。<br> 大数据量删除或更新时，分批次操作以减少锁竞争。<br> TRUNCATE 仅在快速清空表时使用。且无法回滚，需谨慎操作。</li></ol><p><em><strong>表的创建与维护（DDL）</strong></em><br> 一、创建表（CREATE TABLE）</p><ol><li>基本语法</li></ol><blockquote><p>CREATE TABLE 表名 ( 列名1 数据类型 [约束条件], 列名2 数据类型 [约束条件], ... );</p></blockquote><p>示例：</p><blockquote><p>CREATE TABLE employees ( employee_id NUMBER PRIMARY KEY, first_name VARCHAR2(50) NOT NULL, salary NUMBER(10, 2), hire_date DATE );</p></blockquote><ol><li><p>常用数据类型<br> | 数据类型 | 说明 | 示例 |<br> | ---- | ---- | ------- |<br> | NUMBER | 数值类型（可指定精度和小数位数） | NUMBER(10, 2) 表示最多10位，2位小数 |<br> | VARCHAR2 | 可变长度字符串 | VARCHAR2(100) 最多100个字符 |<br> | CHAR | 固定长度字符串 | CHAR(10) 不足10位用空格填充 |<br> | DATE | 日期时间 | 存储日期和时间（如 &#39;2025-05-25&#39;） |<br> | BLOB | 二进制大对象（如图片、文件） | 存储非结构化数据 |</p></li><li><p>约束条件<br> 主键约束（PRIMARY KEY）：唯一标识每条记录，不允许 NULL。<br> 非空约束（NOT NULL）：字段值不能为空。<br> 唯一约束（UNIQUE）：字段值必须唯一。<br> 默认值（DEFAULT）：为字段设置默认值。</p></li></ol><p>二、修改表结构（ALTER TABLE）</p><ol><li>添加列</li></ol><blockquote><p>ALTER TABLE 表名 ADD (新列名 数据类型 [约束条件]);</p></blockquote><p>示例： <code>ALTER TABLE employees ADD (email VARCHAR2(100) UNIQUE NOT NULL,phone VARCHAR(20) DEFAULT &#39;12345678900&#39;, created_at DATE DEFAULT SYSDATE );</code><br> 注意事项：新增列默认值为 NULL，若需非空（NOT NULL），需提供默认值（DEFAULT）。<br> 2. 修改列</p><blockquote><p>ALTER TABLE 表名 MODIFY (列名 新数据类型 [约束条件]);</p></blockquote><p>示例： <code>ALTER TABLE employees MODIFY (salary NUMBER(12, 2));</code><br> 注意事项：修改列类型时，需确保现有数据兼容新类型（如 VARCHAR2 扩展长度无影响，但缩小长度可能导致数据截断）。<br> 3. 删除列</p><blockquote><p>ALTER TABLE 表名 DROP COLUMN 列名;</p></blockquote><p>示例：<code>ALTER TABLE employees DROP COLUMN commission;</code><br> 注意事项：<br> 删除列时，若该列有依赖（如索引、视图、触发器），需先删除依赖对象。<br> 某些数据库（如 Oracle）支持 CASCADE CONSTRAINTS 级联删除关联约束。</p><p>三、删除表（DROP TABLE）</p><blockquote><p>DROP TABLE 表名 [PURGE];</p></blockquote><p>示例：DROP TABLE employees PURGE;<br> 参数说明：PURGE：立即删除表并释放空间（不可恢复）。若不加 PURGE，表可能进入回收站。<br> 注意事项：<br> 删除表会同时删除表中的数据、结构、索引和约束。<br> 若表被其他对象（如视图、存储过程）引用，需先删除依赖对象。</p><p><em><strong>五、注意事项</strong></em></p><ul><li>命名规范：<br> 表名和列名应具有描述性（如 orders 而非 o）。<br> 避免使用保留字（如 user, order）。</li><li>数据类型选择：<br> 优先选择合适的数据类型以节省存储空间（如 CHAR 适合固定长度，VARCHAR2 适合可变长度）。<br> 对日期时间使用 DATE 或 TIMESTAMP。</li><li>约束管理：<br> 主键和唯一约束确保数据唯一性。</li><li>修改表结构的谨慎操作：<br> 修改列类型或长度前，备份数据。<br> 删除列前检查依赖对象（如索引、视图）。</li><li>删除表的不可逆性：<br> 使用 DROP TABLE ... PURGE 前确保已备份重要数据。<br> 生产环境中避免直接删除表，可使用 TRUNCATE 清空数据替代。</li></ul><ol><li><p>用户权限（DCL）<br> (1) 创建用户<br><code>CREATE USER username IDENTIFIED BY password;</code><br> -- 示例：创建用户 finance_user，密码为 secure_pass<br><code>CREATE USER finance_user IDENTIFIED BY secure_pass;</code><br> 注意：用户名需唯一，密码需符合复杂度要求。<br> (2) 授予权限<br> 系统权限：控制用户对数据库整体操作的能力（如连接、创建对象、修改系统参数）。<br> -- 授予用户连接数据库的权限<br><code>GRANT CREATE SESSION TO finance_user;</code><br> -- 授予用户创建表、视图的权限<br><code>GRANT CREATE TABLE, CREATE VIEW TO finance_user;</code><br> -- 授予用户 DBA 角色（最高权限，需谨慎使用）<br><code>GRANT DBA TO finance_user;</code><br> (2) 授予权限<br> 对象权限：控制用户对特定数据库对象（如表、视图）的操作权限。<br> -- 授予用户对 employees 表的 SELECT 和 INSERT 权限<br><code>GRANT SELECT, INSERT ON hr.employees TO finance_user;</code><br> -- 授予用户对 departments 表所有权限（SELECT, INSERT, UPDATE, DELETE 等）<br><code>GRANT ALL ON hr.departments TO finance_user;</code><br> 角色权限：通过角色集中管理权限，简化权限分配。<br> -- 创建角色 FINANCE_READER<br><code>CREATE ROLE finance_reader;</code><br> -- 为角色授予权限<br><code>GRANT SELECT ON hr.employees TO finance_reader;</code><br><code>GRANT SELECT ON hr.departments TO finance_reader;</code><br> -- 将角色赋予用户<br><code>GRANT finance_reader TO finance_user;</code><br> (3) 回收权限<br> -- 回收系统权限<br><code>REVOKE CREATE TABLE FROM finance_user;</code><br> -- 回收对象权限<br><code>REVOKE SELECT ON hr.employees FROM finance_user;</code><br> -- 回收角色权限<br><code>REVOKE finance_reader FROM finance_user;</code><br> -- 删除用户（Oracle 中需加 CASCADE 删除用户所有对象）<br><code>DROP USER finance_user CASCADE;</code><br> 注意事项：<br> 回收权限可能影响依赖该权限的业务逻辑。<br> 使用 CASCADE 删除用户时，会清除用户的所有对象（如表、视图）。</p></li><li><p>事务控制（TCL）<br> (1) 提交与回滚<br> -- 开始事务（隐式启动）<br><code>BEGIN;</code><br> -- 执行操作<br><code>INSERT INTO employees (employee_id, first_name, salary) VALUES (101, &#39;John&#39;, 5000);</code><br><code>UPDATE accounts SET balance = balance - 1000 WHERE account_id = 1;</code><br><code>UPDATE accounts SET balance = balance + 1000 WHERE account_id = 2;</code><br> -- 提交事务（永久保存修改）<br><code>COMMIT;</code><br> -- 回滚事务（撤销所有操作）<br><code>ROLLBACK;</code></p></li></ol><p>(1) 提交与回滚<br> 事务特性（ACID）：</p><ul><li>原子性（Atomicity）：事务内的操作要么全成功，要么全失败。</li><li>一致性（Consistency）：事务执行前后，数据库状态保持一致。</li><li>隔离性（Isolation）：并发事务之间相互隔离。</li><li>持久性（Durability）：提交后的修改永久保存。</li></ul><p>(2) 保存点（SAVEPOINT）<br> 用途：允许部分回滚，避免整个事务被撤销。<br> -- 设置保存点<br><code>SAVEPOINT sp1;</code><br> -- 执行操作<br><code>UPDATE orders SET status = &#39;Shipped&#39; WHERE order_id = 1001;</code><br> -- 回滚到保存点<br><code>ROLLBACK TO sp1;</code><br> -- 释放保存点<br><code>RELEASE SAVEPOINT sp1;</code><br> 综合示例<br> 示例 1：权限管理<br> -- 创建用户并授予权限<br><code>CREATE USER finance_reader IDENTIFIED BY secure_pass;</code><br><code>ALTER USER finance_reader DEFAULT TABLESPACE users QUOTA UNLIMITED ON users;</code><br> -- 创建角色并授予权限<br><code>CREATE ROLE finance_reader_role;</code><br><code>GRANT SELECT ON hr.employees TO finance_reader_role;</code><br><code>GRANT SELECT ON hr.departments TO finance_reader_role;</code><br> -- 将角色赋予用户<br><code>GRANT finance_reader_role TO finance_reader;</code><br> 示例 2：事务控制</p><blockquote><p>-- 转账事务<br> BEGIN;<br> UPDATE accounts SET balance = balance - 1000 WHERE account_id = 1;<br> UPDATE accounts SET balance = balance + 1000 WHERE account_id = 2;<br> COMMIT;<br> -- 异常处理（伪代码）<br> BEGIN TRY<br> BEGIN TRANSACTION;<br> -- 操作<br> COMMIT TRANSACTION;<br> END TRY<br> BEGIN CATCH<br> ROLLBACK TRANSACTION;<br> END CATCH;</p></blockquote><ol><li><em><strong>单行函数：</strong></em><br> 单行函数对每一行数据进行处理并返回单一结果，常用于数据转换、格式化或计算。<br> (1) 字符函数<br> UPPER(str)：将字符串转换为大写 <br> 示例：<code>SELECT UPPER(&#39;hello&#39;) FROM dual;</code> → HELLO<br> LOWER(str)：将字符串转换为小写 <br> 示例：<code>SELECT LOWER(&#39;HELLO&#39;) FROM dual;</code> → hello<br> SUBSTR(str, start, length)：截取子字符串 <br> 示例：<code>SELECT SUBSTR(&#39;abcdef&#39;, 2, 3) FROM dual;</code> → bcd</li></ol><p>(2) 数值函数<br> ROUND(n, decimal)：四舍五入到指定小数位 <br> 示例：<code>SELECT ROUND(123.456, 2) FROM dual;</code> → 123.46<br> TRUNC(n, decimal)：截断到指定小数位 <br> 示例：<code>SELECT TRUNC(123.987) FROM dual;</code> → 123<br> MOD(a, b)：取余运算 <br> 示例：<code>SELECT MOD(10, 3) FROM dual;</code> → 1</p><p>(3) 日期函数<br> SYSDATE ：获取当前系统日期和时间 <br> 示例：<code>SELECT SYSDATE FROM dual;</code><br> ADD_MONTHS(date, n)：在日期上加 n 个月 <br> 示例：<code>SELECT ADD_MONTHS(SYSDATE, 3) FROM dual;</code> → 三个月后的日期<br><code>MONTHS_BETWEEN(date1, date2)</code>：计算两个日期之间的月数差 <br> 示例：<code>SELECT MONTHS_BETWEEN(&#39;2023-01-01&#39;, &#39;2022-01-01&#39;) FROM dual;</code> → 12<br><code>TO_CHAR(date, &#39;format&#39;)</code> ：日期格式化为字符串 <br> 示例：<code>SELECT TO_CHAR(SYSDATE, &#39;YYYY-MM-DD HH24:MI:SS&#39;) FROM dual;</code></p><ol start="2"><li><em><strong>聚合函数与分组查询</strong></em><br> 聚合函数对一组数据进行统计计算并返回单一结果，常与 GROUP BY 结合使用。</li></ol><p>(1) 聚合函数<br> COUNT(*)：统计总行数<br> 示例：<code>SELECT COUNT(*) FROM employees;</code><br> COUNT(column)：统计非空值的行数<br> 示例：<code>SELECT COUNT(manager_id) FROM employees;</code><br> SUM(column)：计算列的总和 <br> 示例：<code>SELECT SUM(salary) FROM employees WHERE department_id = 10;</code><br> AVG(column)：计算列的平均值 <br> 示例：<code>SELECT AVG(salary) FROM employees;</code><br> MIN(column)：获取列的最小值 <br> 示例：<code>SELECT MIN(hire_date) FROM employees;</code><br> MAX(column)：获取列的最大值 <br> 示例：SELECT MAX(salary) FROM employees;<br> (2) 分组查询：通过 GROUP BY 对数据分组后应用聚合函数。<br> 基本语法：</p><blockquote><p>SELECT column1, column2, ..., aggregate_function(column) FROM table_name<br> WHERE condition<br> GROUP BY column1, column2, ...<br> HAVING group_condition<br> ORDER BY column1, column2, ...;</p></blockquote><p>示例<br> --按部门分组统计平均工资<br><code>SELECT department_id, AVG(salary) AS avg_salary FROM employees GROUP BY department_id;</code><br> --按部门和职位分组统计员工数量<br><code>SELECT department_id, job_id, COUNT(*) AS employee_count FROM employees GROUP BY department_id, job_id;</code><br> --使用 HAVING 过滤分组<br><code>SELECT department_id, AVG(salary) AS avg_salary FROM employees GROUP BY department_id HAVING AVG(salary) &gt; 2000;</code><br> 注意事项：<br> SELECT 列限制：除聚合函数外，所有字段必须包含在 GROUP BY 中。<br> -- 正确<br><code>SELECT department_id, job_id, AVG(salary) FROM employees GROUP BY department_id, job_id;</code></p><p>-- 错误（job_id 未在 GROUP BY 中）<br><code>SELECT department_id, job_id, AVG(salary) FROM employees GROUP BY department_id;</code></p><p>WHERE 与 HAVING 的区别：<br> WHERE：在分组前过滤行（不能使用聚合函数）。<br> HAVING：在分组后过滤组（可以使用聚合函数）。</p><p>NULL 值处理：聚合函数自动忽略 NULL 值（如 COUNT(column) 不统计 NULL 行）。</p><p>综合示例<br> 示例 1：单行函数与聚合函数结合<br> -- 查询员工姓名、入职年份及部门编号<br><code>SELECT first_name AS name, TO_CHAR(hire_date, &#39;YYYY&#39;) AS hire_year, department_id FROM employees;</code><br> -- 查询各部门的平均工资（保留两位小数）<br><code>SELECT department_id, ROUND(AVG(salary), 2) AS avg_salary FROM employees GROUP BY department_id;</code><br> 示例 2：分组统计与 HAVING<br> -- 查询员工人数大于 5 的部门<br><code>SELECT department_id, COUNT(*) AS employee_count FROM employees GROUP BY department_id HAVING COUNT(*) &gt; 5;</code></p><p>-- 查询平均工资高于公司平均工资的部门<br><code>SELECT department_id, AVG(salary) AS avg_salary FROM employees GROUP BY department_id HAVING AVG(salary) &gt; (SELECT AVG(salary) FROM employees);</code></p><p>使用建议：<br> 性能优化：<br> 避免在 GROUP BY 中使用不必要的字段。<br> 对分组字段（如 department_id）创建索引以加速查询。<br> 空值处理：<br> 使用 <code>NVL(column, default_value)</code> 替代空值，避免聚合计算错误。<br> 示例：<code>SELECT AVG(NVL(commission_pct, 0)) FROM employees;</code></p><p>内连接（INNER JOIN）与外连接（OUTER JOIN）的核心区别‌在于它们对‌不匹配行的处理方式不同‌：<br> ‌内连接（INNER JOIN）‌：仅返回两个表中‌根据连接条件匹配的行‌，不匹配的行会被完全排除。<br> ‌外连接（OUTER JOIN）‌：除了返回匹配的行外，还会保留‌至少一个表中所有行‌，即使在另一个表中没有匹配项，未匹配部分用 NULL 填充。</p><ol><li><em><strong>多表连接（JOIN）</strong></em><br> (1) 内连接（INNER JOIN）:返回两个表中满足连接条件的行。<br> -- 查询员工及其所属部门名称。<br><code>SELECT e.employee_id, e.name, d.department_name FROM employees e INNER JOIN departments d ON e.department_id = d.department_id;</code><br> 仅返回两表中 department_id 匹配的行，未匹配的记录被过滤。</li></ol><p>(2) 外连接（OUTER JOIN）<br> 左外连接（LEFT JOIN）：返回左表所有行，右表无匹配时填充 NULL。<br> -- 查询所有员工（包括未分配部门的员工），部门字段为 NULL。<br><code>SELECT e.employee_id, e.name, d.department_name FROM employees e LEFT JOIN departments d ON e.department_id = d.department_id;</code></p><p>右外连接（RIGHT JOIN）：返回右表所有行，左表无匹配时填充 NULL。<br> -- 查询所有部门（包括无员工的部门），员工字段为 NULL。<br><code>SELECT e.employee_id, d.department_name FROM employees e RIGHT JOIN departments d ON e.department_id = d.department_id;</code></p><p>全外连接（FULL JOIN）：返回两表所有行，不匹配时填充 NULL。<br> -- 查询所有员工和部门信息，未匹配的部分用 NULL 填充。<br><code>SELECT e.name, d.department_name FROM employees e FULL JOIN departments d ON e.department_id = d.department_id;</code></p><p>(3) 自连接（Self Join）：将表与自身关联，常用于层级数据（如员工与上级关系）。<br> -- 查询员工及其直属上级<br><code>SELECT e1.name AS employee, e2.name AS manager FROM employees e1 INNER JOIN employees e2 ON e1.manager_id = e2.employee_id;</code><br> 通过别名 e1 和 e2 实现表的自关联，查询员工的上级姓名。</p><p>(4) 性能优化建议<br> 数据量较小：优先使用子查询（如 IN 或 EXISTS）。<br> 数据量较大：优先使用 JOIN，并通过 执行计划 分析执行效率。<br> 避免笛卡尔积：确保连接条件明确，防止两表所有行组合（CROSS JOIN）。<br> 索引优化：在连接字段（如 department_id）上创建索引，加速查询。</p><p><em><strong>如何在连接字段上创建索引？‌</strong></em><br> ‌1. 单字段索引‌<br> 适用于单列连接条件：<br> -- 为 orders 表的 user_id 字段创建索引<br><code>CREATE INDEX idx_order_user ON orders(user_id);</code><br><code>ALTER TABLE table_name ADD INDEX idx_name (column_name);</code></p><ol start="2"><li><em><strong>子查询（Subquery）</strong></em><br> 子查询是嵌套在主查询中的查询语句，常用于条件过滤、字段计算或表来源。<br> (1) 标量子查询（Scalar Subquery）：返回单个值（一行一列），可作为字段或条件使用。<br> -- 查询工资高于公司平均工资的员工<br><code>SELECT employee_id, name, salary FROM employees WHERE salary &gt; (SELECT AVG(salary) FROM employees);</code><br> 子查询在主查询前执行一次，结果被主查询复用。</li></ol><p>(2) 行级子查询<br> 单行子查询：返回一行一列，使用单行比较符（&gt;, =, &lt; 等）。<br> -- 查询工资等于 &#39;Abel&#39; 的员工<br><code>SELECT last_name, salary FROM employees WHERE salary = (SELECT salary FROM employees WHERE last_name = &#39;Abel&#39;);</code></p><p>(2) 行级子查询<br> 多行子查询：返回多行一列，需搭配 IN, ANY, ALL 等操作符。<br> -- 查询工资大于部门 10 中任意一人的员工<br><code>SELECT last_name, salary FROM employees WHERE salary &gt; ANY (SELECT salary FROM employees WHERE department_id = 10);</code><br> 操作符：<br> =ANY：等价于 IN。</p><blockquote><p>ANY：大于子查询结果中的某个值。<br> ALL：大于子查询结果中的所有值。</p></blockquote><p>(3) EXISTS 与 IN<br> EXISTS ：判断子查询是否返回非空结果集，效率通常高于 IN。<br> -- 查询已选课程的学生<br><code>SELECT * FROM students s WHERE EXISTS ( SELECT 1 FROM course_selection cs WHERE cs.student_id = s.student_id );</code><br> 一旦找到匹配项立即停止搜索，适合大数据量场景。<br> IN ：匹配字段值是否在子查询结果集中。<br> -- 查询姓张或李的学生<br><code>SELECT * FROM students WHERE last_name IN (&#39;张&#39;, &#39;李&#39;);</code><br> 需先计算子查询结果集，适合结果集较小的场景。</p><p>(4) 子查询建议<br> 避免嵌套过深：复杂子查询可拆分为临时表（WITH 子句）。<br> 性能优化：<br> 使用 EXISTS 替代 IN（尤其在子查询返回大量数据时）。<br> 在子查询字段上创建索引。<br> 子查询需包含在括号内。<br> 单行操作符（如 =）对应单行子查询，多行操作符（如 IN）对应多行子查询。</p><ol start="3"><li>注意事项<br> JOIN 与子查询的选择：<br> JOIN：适合需要组合多表数据的场景（如返回两表字段）。<br> 子查询：适合条件依赖其他表的场景（如过滤或计算字段）。</li></ol><p>性能对比：<br> EXISTS 通常比 IN 更高效，因为它在找到第一个匹配项后立即停止搜索。<br> 对于大数据量，优先使用 JOIN；对于小数据量，使用子查询更直观。</p>',71)]))}const n=r(a,[["render",l],["__file","szbackend.html.vue"]]),i=JSON.parse('{"path":"/szkingdom/szbackend.html","title":"后端","lang":"zh-CN","frontmatter":{"title":"后端","feed":false,"seo":false,"head":[]},"headers":[{"level":2,"title":"需求分析","slug":"需求分析","link":"#需求分析","children":[{"level":3,"title":"需求分析方法","slug":"需求分析方法","link":"#需求分析方法","children":[]}]},{"level":2,"title":"代码规范","slug":"代码规范","link":"#代码规范","children":[]},{"level":2,"title":"数据库","slug":"数据库","link":"#数据库","children":[]}],"git":{"createdTime":1774581624000,"updatedTime":1774581624000,"contributors":[{"name":"tonghaoran","username":"tonghaoran","email":"tonghaoran@szkingdom.com","commits":1,"url":"https://github.com/tonghaoran"}]},"readingTime":{"minutes":18.66,"words":5597},"filePathRelative":"szkingdom/szbackend.md","localizedDate":"2026年3月27日"}');export{n as comp,i as data};
